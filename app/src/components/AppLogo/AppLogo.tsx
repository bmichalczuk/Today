import Logo from "../../styled-components/Logo";
import logoImage from "./checklist-checked-box.png";

const AppLogo = () => {
    return (
        <Logo alt="" src={logoImage} />
    );
};

export default AppLogo;